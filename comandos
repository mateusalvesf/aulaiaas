sudo su -
apt update

curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash - &&\
sudo apt-get install -y nodejs

apt install git

git clone https://github.com/eversonpereira/aulaiaas.git

cd aulaiaas

# Criar o arquivo .env e colocar o conteúdo WEATHERAPI_API_KEY=SuaChaveAqui

npm install
npm start

# Liberar porta no firewall da Azure
