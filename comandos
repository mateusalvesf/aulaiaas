sudo su -
apt update

curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash - &&\
sudo apt-get install -y nodejs

apt install git

git clone https://github.com/eversonpereira/aulaiaas.git

cd aulaiaas
